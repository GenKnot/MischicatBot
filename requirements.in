# 直接依赖。改这里，然后跑：
#   .venv/bin/pip-compile requirements.in -o requirements.txt
#   .venv/bin/pip-compile requirements-dev.in -o requirements-dev.txt
#
# requirements.txt 是生成物，不要手改。

discord.py==2.7.1
davey==0.1.4                       # discord.py 语音 E2EE（DAVE 协议）
PyNaCl==1.6.2

# yt-dlp 刻意不钉死版本。YouTube 的播放器和签名方案变得快，钉住等于几个月后
# 音乐就失效。Dockerfile 里另有一行 `pip install -U yt-dlp`，CI 每周重建镜像
# 时会带上最新版。这里只设下限，防止装到已知失效的老版本。
yt-dlp>=2026.3.3
bgutil-ytdlp-pot-provider==1.3.1

fastapi==0.135.1
uvicorn[standard]==0.41.0
jinja2==3.1.6
python-multipart==0.0.22

sqlalchemy[asyncio]==2.0.48
aiosqlite==0.22.1
alembic==1.19.1

python-dotenv==1.2.2
tzdata==2025.3
