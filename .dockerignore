# 白名单式：先忽略一切，再逐项放行运行时真正需要的东西。
#
# 为什么不用黑名单：黑名单要求每次新增敏感文件时都记得来加一行，
# 迟早会漏。白名单的失败方式是"少打包了某个文件、启动即报错"，
# 比"多打包了 .env"安全得多。
#
# 新增运行时需要的目录/文件时，记得在下面补一条 `!` 放行。

*

# --- 运行时必需 ---
!bot.py
!main.py
!version.py
!requirements.txt
!cogs/
!utils/
!web/
!data/

# --- 上面放行目录后，再剔除目录内不该进镜像的东西 ---
**/__pycache__/
**/*.pyc
**/.DS_Store

# 说明：
# - .env / game.db / cookies.txt / .venv/ 天然被 `*` 挡住
# - .git/ 未放行，因此镜像内不是 git 仓库：AUTO_UPDATE 会打印
#   "Not a git repository; skip" 并跳过。容器本就该靠拉新镜像升级，
#   而不是在容器里 git reset --hard；k8s 里该功能也早已主动禁用。
# - tests/ 未放行：生产镜像不需要。CI 跑测试在构建之前、用源码目录跑。
